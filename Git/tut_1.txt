//
! Git Commands
? Directory -> Folder

TODO : GIT - tracks the changes in code
TODO : GITHUB - a website that hosts our code.

! Clone
Bring a repository that is hosted somewhere like Github into a folder on your local machine.

! add
Track your files and changes in Git

! Commit 
save you file in Git

! push
upload git commits to a remote repo like Github

! pull
Download changes from remote repo to ypur local machine, the opposite of push




TODO : FOR LOCAL MACHINE
? git init
initializes the repository

? git status
to see the status of the repository

? git add <filename>
to stage the file

? git commit -m "Commit message"
To commit the changes
! always use double quotes in windows.


TODO : FOR REMOTE REPOSITORY LIKE GIT HUB
? git remote add origin <repository url>
To add remote repository

? git push -u origin master
To push changes on remote repository

? git remote set-url origin <repository url>
TO replace origin


? git branch -m <branch name>
To create a branch

? git add .
add everything in repository

? git checkout -b <new branch>
To switch branch

? git merge master
to merge with master branch
